programa {
  funcao inicio() {
    real anos, resultado 

    // Entrada
    
    escreva("informe sua idade em anos: ")
    leia(anos)

    // Processamento (resultado = anos * 365)

    resultado = anos * 365

    // Saída

    escreva("\nvocê já viveu ", resultado, " dias.")
  }
}